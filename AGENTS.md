# AGENTS.md

本文件用于约束以后 Codex 在「今日气场 Today Aura」项目中写代码、改文档、设计界面和生成内容时的行为。所有后续工作必须以 `README.md`、`PRODUCT.md`、`MVP_SPEC.md` 和 `ROADMAP.md` 为基础。

## 1. 项目总原则

- 项目定位是「每日审美决策助手」，不是算命产品、不是电商导购、不是身材评判工具。
- 产品核心问题只有一个：帮助用户快速回答「我今天该怎么穿 / 我今天该怎么出现」。
- MVP 阶段不得擅自扩大范围。所有新增功能都必须服务于每日形象决策。
- 第一版只围绕以下能力建设：
  - 用户档案。
  - 今日场景。
  - 今日状态。
  - 颜色推荐。
  - 穿搭建议。
  - 妆容配饰建议。
  - 分享卡片。
- 第一版明确不做：
  - 复杂 3D。
  - 完整衣橱管理。
  - 真实电商交易。
  - 真人身材分析。
  - 真人照片评分。
  - 重度星盘、塔罗或玄学系统。
  - 社区关注、评论、发布流。
- 所有面向用户的文案默认使用中文。
- 所有建议都要降低穿搭焦虑，而不是制造新的外貌焦虑、消费焦虑或选择焦虑。
- 首页第一屏必须直接进入今日建议流程，不做空泛品牌宣传页。

## 2. 产品判断规则

- 判断一个功能是否进入 MVP，先问：它是否能直接帮助用户完成「今天怎么穿 / 今天怎么出现」的决策。
- 任何新功能必须说明它服务于 `PRODUCT_LOGIC.md` 中的哪个产品逻辑环节，例如首页入口、今日输入、生成结果、保存、重新生成或分享卡片。
- 如果答案不明确，默认不放进 MVP。
- 如果功能只是炫技，不能降低用户决策成本，默认不进入 MVP。
- 如果功能主要服务于增长、社区、商业化、复杂技术展示或长期画像沉淀，默认放入后续路线，不进入第一版。
- 如果功能需要用户大量录入，必须证明它不会破坏早晨快速使用场景。
- 关键流程控制在 3 步以内：
  - 用户档案。
  - 今日状态。
  - 生成结果。
- 用户档案是为了让建议更贴近用户，不是为了建立复杂画像系统。
- 今日场景是推荐逻辑的第一优先级，建议不得与场景冲突。
- 今日状态用于调节建议的情绪价值和执行难度，尤其要照顾低精力、焦虑、烦躁等状态。
- 分享卡片是 MVP 的核心输出之一，不是附属装饰。
- ROADMAP 中的能力只代表后续方向，不能在 MVP 中提前实现，除非用户明确要求调整产品范围。

## 3. 内容生成规则

- 内容必须具体、可执行、可解释。
- 如果一句建议听起来好听但用户不能照着做，必须改成具体建议。
- 避免空泛表达，例如：
  - 不要只说「穿得高级一点」。
  - 不要只说「选择适合自己的风格」。
  - 不要只说「打造氛围感」。
- 要改成可执行表达，例如：
  - 「上装选奶白或浅灰针织，搭配直筒深色下装，把视觉重点放在干净领口和小面积金属耳饰。」
  - 「今天会议多，主色用灰蓝，辅助色用炭黑，避免大面积亮橙，整体会更稳定。」
- 每次生成今日建议时，至少包含：
  - 今日气场关键词。
  - 今日主色。
  - 今日辅助色。
  - 今日点缀色。
  - 穿搭建议。
  - 妆容配饰建议。
  - 一句话提醒。
  - 分享卡片摘要。
- 颜色建议必须能映射到真实衣物，不使用过于抽象的颜色命名。
- 主色、辅助色和点缀色不能重复。
- 推荐理由必须引用至少一种输入来源：
  - 用户档案。
  - 今日场景。
  - 天气。
  - 情绪。
  - 精力。
  - 今日目标气场。
- 推荐逻辑必须可解释，不能只返回一段无法追踪来源的文案。
- 点缀色说明不能攻击用户外貌，不能使用以下表达：
  - 显胖。
  - 显黑。
  - 显土。
  - 显老。
  - 暴露缺点。
  - 不适合你的身材。
- 点缀色只能解释为在今天的场景、天气或情绪下不建议大面积使用，例如：
  - 「今天是正式面试场景，大面积高饱和玫红容易抢走表达重点，可以改成小面积配饰。」
  - 「今天情绪偏烦躁，大面积亮橙可能增加视觉噪音，建议用更稳定的灰蓝或米白。」
- 轻量趣味运势只能作为仪式感和情绪引导，不能决定核心建议。
- 不输出绝对化判断，例如「今天一定会遇到好运」或「你今天不适合见人」。
- 不引导购买，不把「买某件商品」作为解决方案。

## 4. 用户体验规则

- 移动端优先，默认用户在手机上早晨快速使用。
- 产品体验要适合 1-3 分钟完成一次生成。
- 今日状态输入应在 30 秒内完成。
- 界面第一屏应直接让用户进入今日建议流程。
- 不做营销式落地页，不用大段品牌故事阻断核心流程。
- 输入项要轻，优先使用按钮、标签、单选、多选、滑杆等低成本交互。
- 默认流程：
  - 如果没有档案，先创建轻量档案。
  - 如果已有档案，直接进入今日状态输入。
  - 生成结果页展示颜色、穿搭、妆容配饰和分享卡片。
- 用户不应被要求上传真人照片才能使用 MVP。
- 用户不应被要求录入完整衣橱才能获得建议。
- 结果页要先给结论，再给解释。
- 颜色和穿搭建议要能快速扫读，避免长段密集文本。
- 精力低时，建议和界面都要降低负担：少步骤、少选项、低维护方案优先。
- 分享卡片内容要短、清晰、适合保存到相册或分享到小红书、朋友圈等场景。

## 5. 视觉设计规则

- 视觉方向是「轻奢色卡 + 日历仪式感 + 小红书分享卡」。
- 设计关键词：
  - 清爽。
  - 精致。
  - 有仪式感。
  - 易扫读。
  - 像每日色卡和形象备忘录。
- 不使用廉价大面积粉色。
- 不做塔罗玄学风。
- 不做淘宝导购风。
- 不做男性效率工具风。
- 不使用过度花哨、廉价贴纸感、强促销感的视觉元素。
- 色彩应围绕今日主色、辅助色、点缀色建立视觉层级。
- 分享卡片的视觉重点是：
  - 日期。
  - 今日气场关键词。
  - 三个颜色色块。
  - 一句话建议。
- 卡片不要堆满长文，不要像报告截图。
- 页面设计应适合手机竖屏。
- 重要按钮要明显，但不能像电商购买按钮。
- 视觉组件应有稳定尺寸，避免内容变化导致布局跳动。
- 不要让文字压住色块、图片或按钮。
- 不要使用纯装饰性的复杂 3D、塔罗牌、星盘、神秘符号作为 MVP 主视觉。

## 6. 技术实现规则

- 先实现静态可用流程，再接入复杂服务。
- 第一版可以使用本地 mock 数据和规则生成，不强依赖后端。
- 推荐逻辑应先用清晰规则、配置和模板实现，后续再考虑 AI 或服务化增强。
- MVP 0.1 不实现真实语音输入，只允许预留 UI 或文档说明。
- 今日特别诉求可以作为可选文本输入，但不作为必填项。
- 颜色、场景、情绪、天气、精力、目标气场等枚举必须集中管理。
- 推荐规则必须结构化，避免散落在多个组件中的硬编码判断。
- 推荐逻辑必须可以追踪到用户输入和规则。
- 生成结果的数据结构至少应能表达：
  - 输入来源。
  - 推荐颜色。
  - 推荐理由。
  - 穿搭建议。
  - 妆容配饰建议。
  - 分享卡片摘要。
- 推荐结果要保留解释字段，方便用户理解，也方便后续调试。
- 不为 MVP 引入复杂状态管理、服务端架构或数据库，除非当前功能确实需要。
- 不实现真实支付、购物车、订单、商品库存等电商能力。
- 不实现复杂 3D 试衣或重型图形渲染。
- 不实现真人身材分析、照片评分或自动容貌判断。
- 如需天气数据，MVP 可先使用用户手动选择或 mock 数据。
- 如需分享卡片，优先实现前端可预览的静态卡片，再考虑图片导出能力。

## 7. 代码质量规则

- 每次开发前先说明将改哪些文件，以及为什么改。
- 修改范围要小，优先完成当前请求，不做无关重构。
- 命名要贴近产品语义，例如 `dailyAura`、`primaryColor`、`accentColor`、`scene`、`mood`。
- 业务规则要有清晰边界，避免把推荐规则写死在 UI 文案里。
- 组件职责要清楚：
  - 输入组件负责收集选择。
  - 推荐逻辑负责生成结构化结果。
  - 展示组件负责渲染结果。
  - 分享卡片组件负责压缩表达。
- 文案、枚举、推荐规则应尽量可维护、可扩展。
- 面向用户的错误提示、空状态和按钮文案必须使用中文。
- 不留下无用代码、调试输出或未使用文件。
- 新增逻辑应包含最低限度的验证方式，例如手动验收步骤、单元测试或页面检查。
- 如果没有运行验证，交付时必须明确说明。

## 8. 隐私与安全规则

- MVP 不要求用户上传真人照片。
- 不采集身材、体重、三围等敏感身体数据。
- 不对用户外貌、身材、肤色做评分或优劣判断。
- 用户档案只收集生成建议所需的轻量信息。
- 不把情绪输入用于恐吓、操控或绝对化预测。
- 不输出医疗、心理诊断或确定性运势判断。
- 不默认接入第三方追踪、广告或电商转化脚本。
- 如果后续需要存储用户数据，必须明确数据用途、最小化字段，并优先保护本地或账户内隐私。
- 分享卡片默认不暴露敏感个人信息。

## 9. 开发节奏

- 开发顺序遵循：
  - 文档确认。
  - 静态原型。
  - 本地 mock 流程。
  - 规则生成。
  - 分享卡片。
  - 体验打磨。
  - 再考虑服务接入。
- 每次开发前必须先对齐本次目标和修改文件。
- 每次开发产品流程、推荐逻辑、页面跳转、输入输出、保存逻辑前，必须先阅读 `PRODUCT_LOGIC.md`。
- 如果用户要求的内容会扩大 MVP，先说明风险，并建议放入 ROADMAP。
- 如果用户明确要求突破 MVP，需要同时更新相关文档，避免代码和产品边界不一致。
- 优先交付可点击、可完成、可验证的流程。
- 不为了技术展示牺牲用户早晨快速使用的体验。
- 每次完成后说明：
  - 已改内容。
  - 未做内容。
  - 如何验证。
  - 下一步建议。

## 10. 每次交付后的自查清单

交付前必须逐项检查：

- 是否仍然围绕「今天怎么穿 / 今天怎么出现」。
- 是否符合 `PRODUCT_LOGIC.md`。
- 是否擅自扩大了 MVP 范围。
- 是否把 MVP 1.1 的语音输入错误地做进 MVP 0.1。
- 是否让用户输入负担变重。
- 是否只覆盖用户档案、今日场景、颜色推荐、穿搭建议、妆容配饰、分享卡片等第一版核心能力。
- 是否误做了复杂 3D、完整衣橱管理、真实电商交易或真人身材分析。
- 面向用户的文案是否都是中文。
- 建议是否具体可执行，而不是只好听。
- 点缀色说明是否避免了「显胖、显黑、显土」等攻击性表达。
- 推荐理由是否能追踪到用户档案、场景、天气、情绪、精力或目标气场。
- 关键流程是否控制在 3 步以内。
- 首页第一屏是否直接进入今日建议流程。
- 移动端是否优先，手机竖屏是否可用。
- 视觉是否符合「轻奢色卡 + 日历仪式感 + 小红书分享卡」。
- 是否避免了廉价大面积粉色、塔罗玄学风、淘宝导购风和男性效率工具风。
- 枚举是否集中管理。
- 推荐逻辑是否可解释、可维护。
- 是否避免采集不必要的敏感信息。
- 是否说明了已改内容、未做内容、验证方式和下一步建议。
