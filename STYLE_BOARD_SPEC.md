# `/style-board` 页面规格

本文件定义 `/style-board` 页面应展示的视觉模块。该页面用于集中检查「今日气场 Today Aura」的视觉系统、组件状态、色卡表达、页面片段和分享卡片方向。

本文件只定义页面内容和验收标准，不要求现在实现页面代码。

## `/style-board` 页面目标

`/style-board` 是项目的视觉板页面，用于回答：

- 当前视觉是否符合「轻奢色卡 + 日历仪式感 + 小红书分享卡」。
- 基础色、字体、圆角、阴影和按钮是否统一。
- 主色、辅助色、点缀色是否清楚可辨。
- 首页、用户档案页、今日输入页、结果页、分享卡片是否属于同一个产品。
- 页面是否移动端优先，适合早晨 1-3 分钟快速使用。

页面不承担真实业务流程，不接后端，不做推荐逻辑。它只展示视觉系统和关键组件样式。

## 页面整体结构

建议按以下顺序展示：

1. 页面标题和视觉方向。
2. 基础色板。
3. 字体层级。
4. 按钮状态。
5. 标签状态。
6. 今日主色 / 辅助色 / 点缀色组件。
7. 用户档案卡片。
8. 今日输入卡片。
9. 结果页卡片。
10. 分享卡片 3:4 / 9:16。
11. 动效示例。
12. 自查标准。

页面顶部应明确写出视觉方向：

> 轻奢色卡 + 日历仪式感 + 小红书分享卡

## 基础色板

基础色板模块用于展示产品界面底色、文字色、边框色和强调色。

必须展示：

| 颜色名 | 色值 | 用途 |
| --- | --- | --- |
| 奶油白 | `#F8F3EA` | 页面主背景 |
| 柔白 | `#FFFCF7` | 卡片背景 |
| 暖雾灰 | `#EFE7DC` | 分区背景 |
| 墨茶黑 | `#292521` | 主标题、重点文字 |
| 暖灰棕 | `#5E564F` | 正文 |
| 雾灰 | `#9B9288` | 辅助文字 |
| 米灰线 | `#E2D8CB` | 边框、分割线 |
| 香槟金 | `#B99A63` | 重点强调、主按钮 |
| 玫瑰雾 | `#D8A7A0` | 小面积温柔提示 |
| 灰蓝 | `#8EA1A8` | 冷静、正式场景辅助 |
| 炭褐 | `#3C3630` | 深色按钮、压轴信息 |

展示方式：

- 每个颜色用独立色块卡片展示。
- 色块下方显示颜色名、色值和用途。
- 深色和浅色都要测试文字对比度。
- 玫瑰雾只能作为小面积提示色展示，不应铺满页面。

## 字体层级

字体层级模块用于检查标题、正文、辅助文字和按钮文字的比例。

必须展示：

- 超级标题：用于结果页气场标题和分享卡片主标题。
- 页面标题：用于首页、输入页、档案页标题。
- 分区标题：用于色彩建议、穿搭建议、妆容配饰。
- 正文：用于建议解释。
- 辅助文字：用于日期、提示、说明。
- 按钮文字：用于主按钮和次按钮。

示例文案：

- 超级标题：清透专注。
- 页面标题：今天想以什么状态出现？
- 分区标题：今日主色。
- 正文：灰蓝能降低通勤场景中的视觉噪音，让整体状态更稳定。
- 辅助文字：6 月 1 日 · 周一。
- 按钮文字：生成今日气场。

展示规则：

- 每个层级标注字号、字重和行高。
- 中文正文必须可读，不使用过细字重。
- 不使用负字距。
- 分享卡片标题不应撑破卡片。

## 按钮状态

按钮状态模块用于展示主要操作和辅助操作。

必须展示：

- 主按钮默认态：生成今日气场。
- 主按钮悬停态：颜色略深或轻微浮起。
- 主按钮点击态：轻微压下。
- 主按钮禁用态：提示未完成必要选择。
- 次按钮默认态：查看最近结果。
- 文本按钮：编辑档案。
- 图标按钮：保存、分享、返回、关闭。

视觉规则：

- 主按钮高度移动端不低于 48 像素。
- 主按钮不能像电商购买按钮。
- 禁用态不能完全消失，应保留下一步提示。
- 所有按钮圆角、字号、内边距保持统一。

## 标签状态

标签状态模块用于展示场景、天气、心情、气质等选择项。

必须展示：

- 默认态：通勤。
- 选中态：清冷。
- 推荐态：适合今天。
- 禁用态：暂不可选。
- 多选组合：奶油白、灰蓝、炭黑。

展示分组：

- 场景标签：通勤、约会、聚会、休闲、面试。
- 天气标签：晴、阴、雨、冷、热。
- 心情标签：平静、低落、焦虑、期待。
- 气质标签：温柔、清冷、元气、松弛、知性。

视觉规则：

- 选中态明显但不刺眼。
- 标签像胶囊色签，不像后台筛选器。
- 移动端标签高度不低于 40 像素。
- 同组标签间距统一。

## 今日主色 / 辅助色 / 点缀色组件

该模块是 `/style-board` 的核心展示区。

必须展示三种色卡：

- 今日主色。
- 今日辅助色。
- 今日点缀色。

示例内容：

| 类型 | 颜色名 | 示例说明 |
| --- | --- | --- |
| 今日主色 | 灰蓝 | 今天是通勤场景，灰蓝能让整体状态更冷静、清爽、有秩序。 |
| 今日辅助色 | 奶油白 | 用在内搭或包包上，可以提亮但不抢走主色。 |
| 今日点缀色 | 亮橙 | 今天情绪偏烦躁，大面积亮橙容易增加视觉噪音，建议只做小面积点缀。 |

组件要求：

- 主色色卡最大，必须成为视觉中心。
- 辅助色和点缀色可以并列展示。
- 每张色卡都要显示颜色名、角色、色块、理由和使用方式。
- 点缀色不得用攻击性表达，不得出现「显胖、显黑、显土」。
- 色卡文本不能压住色块。

## 用户档案卡片

用户档案卡片用于展示个人审美基线。

必须展示：

- 昵称或称呼：早安，Cary。
- 常见场景：通勤、休闲、约会。
- 喜欢风格：清冷、松弛、知性。
- 常穿色：奶油白、炭黑、灰蓝。
- 避开颜色：荧光粉、高饱和橙。
- 希望气质：干净、稳定、有边界。
- 编辑档案入口。

视觉规则：

- 卡片应像轻量个人色卡，不像用户资料后台。
- 信息用标签和色块表达，不用复杂表格。
- 编辑入口弱化，不抢主流程。
- 卡片可用于首页档案摘要和档案页顶部摘要。

## 今日输入卡片

今日输入卡片用于展示用户当天状态选择。

必须展示四组输入：

- 场景：今天主要去哪？
- 天气：外面是什么状态？
- 心情：你现在感觉如何？
- 想要气质：今天想被怎样看见？

示例选中：

- 场景：通勤。
- 天气：阴。
- 心情：有点焦虑。
- 想要气质：清冷。

视觉规则：

- 每组是独立卡片或清晰分区。
- 当前选中值要有明显视觉反馈。
- 顶部可展示一句状态摘要：「通勤 · 阴天 · 有点焦虑 · 想要清冷感」。
- 底部展示主按钮「生成今日气场」。
- 组件不能像普通问卷表单。

## 结果页卡片

结果页卡片用于展示生成后的核心信息层级。

必须展示：

- 气场标题：清透专注。
- 主色色卡：灰蓝。
- 辅助色：奶油白。
- 点缀色：亮橙。
- 颜色解释：说明来自通勤场景、阴天、焦虑心情和清冷目标气质。
- 穿搭建议：上装、下装、外套、鞋包、替代方案。
- 妆容配饰：妆感、唇色、发型、首饰、小物。
- 今日短句：把重点留给自己，不必用力证明。
- 操作：保存卡片、重新生成。

视觉规则：

- 气场标题和主色必须在首屏可见。
- 穿搭建议和妆容配饰用短句和分组，不写成长文章。
- 解释必须可追踪到输入来源。
- 保存和重新生成按钮要清楚，但不能像购物转化按钮。

## 分享卡片 3:4 / 9:16

该模块用于展示两种分享卡片比例。

### 3:4 卡片

用途：

- 结果页内预览。
- 保存到相册。
- 小红书图文中的单张卡片。

必须包含：

- 今日气场 Today Aura。
- 日期。
- 气场标题。
- 主色、辅助色、点缀色。
- 穿搭关键词。
- 今日短句。

布局建议：

- 顶部：产品名和日期。
- 中部：气场标题和主色色块。
- 下部：辅助色 / 点缀色小色块、穿搭关键词、今日短句。

### 9:16 卡片

用途：

- 朋友圈长图。
- 小红书封面延展。
- 故事流分享。

必须包含：

- 与 3:4 卡片相同的信息。
- 可增加更多留白和纵向节奏。
- 不增加长段解释。

视觉规则：

- 两种比例应属于同一套视觉系统。
- 主色仍然是视觉中心。
- 卡片不能像截图拼贴。
- 底部品牌标识要克制。
- 不放价格、商品图、二维码或营销话术。

## 动效示例

`/style-board` 页面应展示或描述以下动效。

动效模块：

- 页面淡入：页面内容进入时 150-250 毫秒淡入。
- 卡片轻微上浮：悬停或选中时上移 2-4 像素。
- 色卡轻微展开：主色卡生成时高度或透明度柔和展开。
- 结果生成过渡：从输入摘要过渡到结果卡片，250-350 毫秒。
- 按钮点击反馈：点击时轻微压下，100-180 毫秒。

禁止展示：

- 粒子动画。
- 大幅旋转。
- 过度弹跳。
- 闪光炫彩。
- 影响阅读的循环动画。

动效说明必须标注：

- 使用场景。
- 时长。
- 缓动方向。
- 是否会影响阅读。

## 自查标准

`/style-board` 完成后必须逐项检查：

- 是否移动端优先？
- 是否降低了用户选择成本？
- 是否能帮助用户完成「今天怎么出现」的决策？
- 是否有保存和分享价值？
- 是否符合「轻奢色卡 + 日历仪式感 + 小红书分享卡」？
- 是否避免廉价大面积粉色？
- 是否避免塔罗、占卜、水晶球、神秘紫玄学风？
- 是否避免淘宝导购页和商品堆叠感？
- 是否避免男性效率工具风？
- 是否避免普通问卷表单和后台管理系统感？
- 色彩、字号、留白、圆角、按钮状态是否统一？
- 主色、辅助色、点缀色是否清楚可辨？
- 点缀色说明是否避免攻击用户外貌？
- 分享卡片是否适合保存到相册或发小红书 / 朋友圈？
- 页面是否能在早晨 1-3 分钟内完成使用？
- 是否没有引入复杂 3D、完整衣橱、真实电商交易或真人身材分析？

